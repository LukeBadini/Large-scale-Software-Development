Implementation of the grid button:

-All of my code is within the Editor.java file
-When you click on the grid button:
  -If there is no grid, draws the grid behind all the shapes
  -If there is a grid, removes the grid